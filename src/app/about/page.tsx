'use client'

import { gsap } from 'gsap'
import { ScrollTrigger } from 'gsap/ScrollTrigger' // ScrollTrigger 가져오기

export default function About() {
  return <main className='mg-0 p-0 box-border'>About 페이지</main>
}
